# Guide Final - Import D&D Beyond ✅

## 🎯 Vous avez trouvé les données !

Vous voyez dans le Preview :
- `id: 37185700`
- `username: "Gilthanas_du_Cormyr"`
- `isAssignedToPlayer: true`
- Message: "Character successfully received."

**C'est la bonne requête !** Mais pour copier TOUT le JSON, suivez ces étapes :

## 📋 Étapes finales :

### 1. Aller dans l'onglet "Response"

Dans le panneau à droite, **cliquez sur l'onglet "Response"** (à côté de "Preview").

⚠️ **Important** : L'onglet "Preview" montre seulement un aperçu partiel. L'onglet "Response" contient le JSON COMPLET que vous devez copier.

### 2. Copier tout le JSON

Dans l'onglet "Response" :
- Cliquez dans la zone de texte du JSON
- **Mac** : `Cmd+A` pour tout sélectionner
- **Windows** : `Ctrl+A` pour tout sélectionner
- Faites défiler vers le bas pour vérifier que tout est sélectionné (le JSON peut être très long)
- **Mac** : `Cmd+C` pour copier
- **Windows** : `Ctrl+C` pour copier

### 3. Importer dans WarChain Arena

1. Allez sur votre application WarChain Arena
2. Page **Personnages** → Cliquez sur **"+ Importer un personnage"**
3. Cliquez sur l'onglet **"Depuis JSON"**
4. Collez le JSON dans la zone de texte :
   - **Mac** : `Cmd+V`
   - **Windows** : `Ctrl+V`
5. Cliquez sur le bouton **"Importer"**

### 4. Vérification

Après l'import, vous devriez voir votre personnage "Gilthanas_du_Cormyr" dans la liste des personnages !

## 💡 Astuce

Si le JSON est très long et que la sélection ne fonctionne pas bien :
- Essayez de cliquer au début du JSON, puis `Shift+Click` à la fin pour sélectionner
- Ou utilisez `Cmd+A` / `Ctrl+A` plusieurs fois
- Certains navigateurs ont un bouton "Copy" dans l'onglet Response - utilisez-le si disponible

## ✅ C'est tout !

Vous avez maintenant toutes les étapes pour importer votre personnage D&D Beyond dans WarChain Arena.
